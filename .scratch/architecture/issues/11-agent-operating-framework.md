# Agent operating framework

Type: grilling
Blocked by: 08, 10

## Question

What does an agent need to read and do to change note-machine safely — a repo-level `CLAUDE.md`, how `CONTEXT.md` / ADRs / `docs/architecture.md` are used and kept current, per-module guidance, test layers (unit tests for pure state logic vs eval replays), and the definition of done for a change?

## Context

- Only the parent `~/hobbes/CLAUDE.md` (generic rules) applies today; no repo-level guide.
- Use `/codebase-design` for what makes modules AI-navigable.
